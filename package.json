{
  "name": "threejscourse",
  "version": "1.0.0",
  "description": "Scène Three.js avec modèles FBX",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy-models",
    "copy-models": "mkdir -p dist/models && cp -r models/* dist/models/",
    "preview": "vite preview",
    "start": "node server.js"
  },
  "dependencies": {
    "three": "^0.157.0",
    "express": "^4.18.2",
    "terser": "^5.31.0"
  },
  "devDependencies": {
    "@types/three": "^0.157.0",
    "vite": "^5.4.16"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
